

public class NWW {
	NWD nwd = new NWD();
	public int nww(int a, int b){
		if (a <= 0 || b <= 0){
			throw new IllegalArgumentException();
		}
		else {
			return Math.abs(a * b) / nwd.nwd(a, b);
		}
	}
}

